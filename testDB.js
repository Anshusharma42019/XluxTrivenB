import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); mongoose.connect(process.env.MONGODB_URL).then(async () => { console.log('Connected'); const db = mongoose.connection.db; const orders = await db.collection('shipmaxxorders').find({ status: { $regex: 'delivered', $options: 'i' } }).toArray(); console.log(JSON.stringify(orders.slice(0, 3), null, 2)); process.exit(0); }).catch(console.error);
