import mongoose from 'mongoose'; import { ShipmaxxOrder } from './src/modules/shipmaxx/models/shipmaxxOrder.model.js'; import dotenv from 'dotenv'; dotenv.config({path: './.env'}); mongoose.connect(process.env.MONGODB_URL).then(async () => { const orders = await ShipmaxxOrder.find().limit(20); console.log('Shipmaxx Orders statuses:'); orders.forEach(o => console.log('ID:', o._id, 'Status:', o.status, 'Source:', o.source_order_id, 'Lead:', o.lead_id)); process.exit(0); }).catch(console.error);
