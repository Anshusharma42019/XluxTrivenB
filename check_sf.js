import dotenv from 'dotenv'; dotenv.config({ path: '.env' }); import mongoose from 'mongoose'; import { Order as ShiprocketOrder } from './src/modules/shiprocket/models/order.model.js'; mongoose.connect(process.env.MONGODB_URL).then(async () => { const o = await ShiprocketOrder.findOne({ awb_code: 'SF3573487644LOS' }); console.log(o ? 'EXISTS' : 'DOES NOT EXIST'); process.exit(0); });
